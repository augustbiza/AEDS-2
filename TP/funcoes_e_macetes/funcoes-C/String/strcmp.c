//strcmp
//strcmp(string, string)

bool compara(char* str, char*modelo) {

    bool igual = false;

    

    return igual;
}

int main(void) {

    char* str;

    do {

        scanf(" %[^\n]", str);

        if(!comparaFIM(str, )) {
            //instruções
        }
    }while(!comparaFIM(str))
}