void mostrarArray(int arr[tam], int tam) {
    
    printf("Array: %d elementos\n", tam);
        
        for(int i = 0; i < tam; i++) {
            
            printf("%d ", arr[i]);
        }
        
        printf("\n");
}